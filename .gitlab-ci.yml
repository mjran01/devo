pages:
  image: alpine:latest
  script:
    - mkdir -p public
    - cp index.html public/
    - cp -r assets public/assets
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"